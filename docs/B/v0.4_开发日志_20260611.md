# v0.4 开发日志 2026-06-11

## 本次完成

- 删除 [virtual_students.csv](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/data/task-a/virtual_students.csv) 中三餐到达时刻列：
  - `breakfast_arrival_minute`
  - `lunch_arrival_minute`
  - `dinner_arrival_minute`

- 更新 [v0.4_开发TODO.md](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/docs/B/v0.4_开发TODO.md)，收敛为更短的执行清单，并补充：
  - 参数化分流策略
  - 随机搜索 / 模拟退火优化
  - 优化接口与后端拆分建议

- 改造 [SeedDataService.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/taska/service/SeedDataService.java)：
  - 适配新的 `virtual_students.csv`
  - 新增 `arrival_rules.csv` 读取
  - 新增 `tag_mappings.csv` 读取
  - 新增 `ArrivalRuleSeed`、`TagMappingSeed` 等种子结构

- 重写 [ArrivalCurveGenerator.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/taska/service/ArrivalCurveGenerator.java)：
  - 去掉硬编码曲线
  - 改为基于 `arrival_rules.csv` 生成分钟级到达分布

- 改造 [SimulationService.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/taska/service/SimulationService.java)：
  - 仿真前先按规则生成人流曲线
  - 再从学生池抽样并分配到各分钟
  - 删除对学生固定到达时刻字段的依赖
  - 接入新的标签标准化服务

- 新增 [TagNormalizationService.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/common/TagNormalizationService.java)：
  - 使用 `tag_mappings.csv` 做精确映射和关键词兜底映射

- 改造 [RecommendationService.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/recommendation/service/RecommendationService.java)：
  - 切换到 `TagNormalizationService`
  - 删除旧的 [TagMatcher.java](/e:/CS/SoftwareDesignTraining/BJTU-Dining-Simulation-System/backend/src/main/java/com/bjtu/dining/recommendation/service/TagMatcher.java)

## 验证

- 执行 `mvn -q -DskipTests compile`，编译通过。
- 使用 `spring-boot:run` 启动后端验证，`GET /api/v1/presets/scenarios` 返回 `200`。

## 当前状态

- 后端已适配新的学生池数据结构。
- 到达曲线已从“学生自带时间 + 硬编码扰动”切换为“`arrival_rules.csv` 驱动的人流分布”。
- 标签标准化已开始从硬编码迁移到 `tag_mappings.csv`。

## 下一步

- 精简 DTO 和旧接口字段
- 增加到达曲线 / 分钟级指标接口
- 参数化分流策略
- 增加单次评估接口
- 接入随机搜索与模拟退火优化流程
