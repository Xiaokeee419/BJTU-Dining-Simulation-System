# 本次修改汇总

日期：2026-05-30  
当前分支：`feature/cohort-replay-diversion-tuning`

## 1. 本次修改目标

本次改动围绕“未分流 baseline”和“分流 compare”的可信度与展示效果展开，目标有三项：

1. 让 `baseline` 与 `compare` 基于同一批用户重放，避免二次抽样带来的结果波动。
2. 让未分流仿真更容易形成“热门窗口更拥挤、部分窗口更空闲”的状态。
3. 让分流建议从“硬阈值拥挤判断”改为“相对压力均衡”。

## 2. 实际修改内容

### 2.1 后端仿真重构

文件：

- `backend/src/main/java/com/bjtu/dining/taska/service/SimulationService.java`

主要改动：

- baseline 运行时只抽样一次用户 cohort，并以 `runId` 关联缓存。
- compare 运行时复用 `baseRunId` 对应的 cohort，从 `minute=0` 重新完整跑一遍仿真。
- cohort 中固定保存：
  - 到达时间
  - 预算范围
  - 等待容忍度
  - 口味偏好
  - 热门偏好
  - 排队厌恶程度
  - 接受分流倾向
  - 决策采样种子
- 窗口选择逻辑从“选最高分窗口”改为“带固定种子的加权抽样”。
- 等待惩罚改为分段函数，前期更弱，后期快速增强。
- 菜品选择也改为固定种子的加权选择，避免 replay 过程中随机序列偏移。
- 分流生效后：
  - 已在队列中的人按聚合方式做部分转移
  - 新到达的人按接受概率决定是否遵循分流建议

结果：

- compare 真正变成“同一批人群重放对比”。
- baseline 更容易形成热点聚集，而不是过早被排队惩罚自动均衡。

### 2.2 分流建议逻辑调整

文件：

- `backend/src/main/java/com/bjtu/dining/recommendation/service/RecommendationService.java`

主要改动：

- 不再要求“源窗口必须高于目标拥挤等级阈值”。
- 不再要求“目标窗口必须处于 IDLE/NORMAL”。
- 对所有营业窗口计算压力分数，综合考虑：
  - 等待时间
  - 队列长度
  - 服务率
  - 当前拥挤等级
- 源窗口从压力明显高于平均值的窗口中选取。
- 目标窗口从压力低于源窗口且仍有承接余量的窗口中选取。
- 建议分流人数按以下因素综合估算：
  - 源目标压力差
  - 队列差
  - 目标承接余量
  - 源窗口超额队列
- 同一个目标窗口允许承接多个源窗口，但会扣减剩余容量，避免过量灌入。

结果：

- 分流建议更贴近“从相对更忙转向相对更闲”。
- 即使不是极端拥挤窗口，也可能被识别为可优化对象。

### 2.3 前端最小调整

文件：

- `frontend/src/components/ScenarioPanel.vue`

主要改动：

- 虚拟就餐人数滑块范围从 `100-2000` 调整为 `300-1800`。
- `EXTREME` 和 `IDLE` 的提示阈值同步下调/收紧。

原因：

- 太低的人数通常不容易稳定展示分流效果。
- 太高的人数容易把所有窗口一起推高，不利于观察“相对均衡”。

## 3. 新增文档

文件：

- `docs/public/分流重放与热门窗口改造说明_20260530.md`
- `docs/public/本次修改汇总_20260530.md`

说明：

- 前者偏方案说明和设计动机。
- 本文档偏交付视角，汇总实际修改内容、验证情况和影响范围。

## 4. 影响范围

本次主要影响以下文件：

- `backend/src/main/java/com/bjtu/dining/taska/service/SimulationService.java`
- `backend/src/main/java/com/bjtu/dining/recommendation/service/RecommendationService.java`
- `frontend/src/components/ScenarioPanel.vue`
- `docs/public/分流重放与热门窗口改造说明_20260530.md`

接口形状未变，仍兼容当前前端调用方式：

- `POST /api/v1/simulations/run`
- `POST /api/v1/strategies/diversion-comparison`
- `GET /api/v1/simulations/{runId}`

## 5. 验证结果

已完成的验证：

- 后端编译通过：`mvn -DskipTests compile`
- 前端构建通过：`npm run build`
- 真实后端接口验证通过：
  - baseline 可正常运行
  - diversion comparison 可正常运行
  - 同一个 `baseRunId` 连续运行两次 compare，建议数量和关键指标变化一致

实际观察到的结果：

- 在 `baseRunId=10013` 的验证中，两次 compare 都生成 `8` 条建议。
- 两次 compare 的关键结果一致：
  - `maxQueueDelta = -2`
  - `busyWindowDelta = 0`
- 说明 compare 已经具备稳定 replay 特征，而不是重新抽样后的偶然结果。

## 6. 远端状态

本分支已推送到远端：

- `origin/feature/cohort-replay-diversion-tuning`

已有功能提交：

- `a0f674e` `Implement cohort replay diversion tuning`

本文档会作为后续补充说明继续推送到同一分支。
